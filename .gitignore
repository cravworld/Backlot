# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# env files (never commit secrets)
.env
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
/prisma/migrations/dev.db*

# document/media store — encrypted blobs written to local disk in dev.
# Never committed: this is exactly the "at rest" data envelope encryption
# protects, and it has no business in git history even encrypted.
/storage/
